/*Template Literals and multi line string: Template literals allow for string interpolation and multiline strings using backticks (`).

*/
const name = "John";
const greeting = `Hello, ${name}!`;
const greetings = `Hello, 
${name}!`;
console.log(greetings)