/*Modules: ES6 introduced a standardized module system using import and export statements, allowing for better code organization and reuse.
modules are typically used in environments like Node.js or modern web browsers that support ES6 module syntax.
*/
// Exporting module
export function add(a, b) {
    return a + b;
  }
  
  // Importing module
  import { add } from "./math"; 